fn main(x:[int],y:int)->int {
	let t : int = if y - x[0] + x[1] then
	{
		y + (y*3) - 1
	} else
	{
		x[0] = 5 + x[0] * if x[0] > 4 then {x[1]} else {main(x,y*4+1)};
		let z : int = x[0] / 5 / 3;
		let ww : int = 5 / z * x[1] - x[z] * z / 14;
		y + z * ww - x[2] / ww * y * x[ww]
	};
	t + t + t + t + t + t * t * t * t * t * t / t / t / t - t
}