fn fib(n: int) -> int {
    let a : int = 1;
    let b : int = 1;
    let i : int = 0;
    while (i < n) {
        let c: int = a + b;
        a = b;
        b = c;
        i = i + 1
    };
    a
}

fn main() -> unit {
    print_int(fib(10));
    print_ln()
}